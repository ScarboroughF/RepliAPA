% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch.R
\name{repliapa_fit_targets}
\alias{repliapa_fit_targets}
\title{Fit multiple perturbation targets with explicit support accounting}
\usage{
repliapa_fit_targets(
  counts,
  sample_data,
  pas_data,
  targets = NULL,
  gene = "gene",
  replicate = "replicate",
  guide = "guide",
  target_col = "target",
  ntc = "ntc",
  replicates = NULL,
  pseudocount = 0.5,
  min_gene_total = 20,
  max_combinations = 1e+06,
  strict = FALSE
)
}
\arguments{
\item{counts}{PAS-by-guide-replicate pseudobulk matrix.}

\item{sample_data}{Sample metadata aligned to columns of \code{counts}.}

\item{pas_data}{PAS metadata aligned to rows of \code{counts}. It must contain a
gene column and may contain \code{position} and \code{strand}.}

\item{targets}{Target labels to fit. By default, all non-NTC target labels.}

\item{gene, replicate, guide, target_col, ntc}{Column names.}

\item{replicates}{Optional biological replicates to include.}

\item{pseudocount}{PAS-count pseudocount used before composition and square root.}

\item{min_gene_total}{Minimum total PAS count in the tested context.}

\item{max_combinations}{Maximum number of exhaustively enumerated admissible
guide-label permutations.
Genes exceeding it abstain; labels are never sampled or split silently.}

\item{strict}{If \code{TRUE}, stop at the first unsupported target. Otherwise,
record it in \code{accounting} and continue.}
}
\value{
A \code{repliapa_fit_list} containing named fits and target accounting.
}
\description{
Fit multiple perturbation targets with explicit support accounting
}
