% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orthogonal-validation.R
\name{repliapa_orthogonal_validation}
\alias{repliapa_orthogonal_validation}
\title{Validate frozen APA directions against an orthogonal count assay}
\usage{
repliapa_orthogonal_validation(
  effect_table,
  orthogonal_data,
  freeze,
  orthogonal_registry_id,
  control = "NT",
  targets = NULL,
  construct_columns = c("gene_id", "pas_id", "aim", "subaim"),
  replicate = "replicate",
  perturbation = "perturbation",
  proximal = "proximal",
  distal = "distal",
  total = "total",
  experiment = NULL,
  experiment_col = "experiment",
  min_replicates = 2L
)
}
\arguments{
\item{effect_table}{Output from \code{\link[=repliapa_proximal_directions]{repliapa_proximal_directions()}} or a data
frame with \code{target}, \code{gene}, \code{proximal_direction}, \code{q_value}, and \code{tested}.}

\item{orthogonal_data}{Row-level orthogonal proximal/distal count data.}

\item{freeze}{Verified analysis freeze created before orthogonal outcomes.}

\item{orthogonal_registry_id}{Registry entry carrying the orthogonal role.}

\item{control}{Perturbation label identifying the orthogonal control.}

\item{targets}{Optional perturbations to validate. Defaults to effect targets.}

\item{construct_columns}{Columns uniquely defining an orthogonal construct;
the first must map to the \code{gene} column in \code{effect_table}.}

\item{replicate, perturbation, proximal, distal, total}{Column names.}

\item{experiment}{Optional scalar experiment value to retain.}

\item{experiment_col}{Column containing the experiment label.}

\item{min_replicates}{Minimum paired orthogonal replicates per construct.}
}
\value{
A \code{repliapa_orthogonal_validation} object with construct-level
results, target summaries, and mapping/support accounting.
}
\description{
This function is deliberately post-freeze. It aggregates raw orthogonal
rows within construct, replicate, and perturbation; pairs each perturbation
with the control in the same orthogonal replicate; and compares only effect
directions. Orthogonal outcomes never refit the APA model.
}
