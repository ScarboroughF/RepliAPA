% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proximal-distal.R
\name{repliapa_fit_proximal_distal}
\alias{repliapa_fit_proximal_distal}
\title{Fit the direction-aware proximal/distal baseline}
\usage{
repliapa_fit_proximal_distal(
  counts,
  sample_data,
  pas_data,
  target,
  gene = "gene",
  position = "position",
  strand = "strand",
  replicate = "replicate",
  guide = "guide",
  target_col = "target",
  ntc = "ntc",
  replicates = NULL,
  pseudocount = 0.5,
  min_gene_total = 20,
  max_combinations = 1e+06
)
}
\arguments{
\item{counts}{PAS-by-guide-replicate pseudobulk matrix.}

\item{sample_data}{Sample metadata aligned to columns of \code{counts}.}

\item{pas_data}{PAS metadata aligned to rows of \code{counts}. It must contain a
gene column and may contain \code{position} and \code{strand}.}

\item{target}{Target label to test.}

\item{gene, replicate, guide, target_col, ntc}{Column names.}

\item{position, strand}{Column names in \code{pas_data}.}

\item{replicates}{Optional biological replicates to include.}

\item{pseudocount}{PAS-count pseudocount used before composition and square root.}

\item{min_gene_total}{Minimum total PAS count in the tested context.}

\item{max_combinations}{Maximum number of exhaustively enumerated admissible
guide-label permutations.
Genes exceeding it abstain; labels are never sampled or split silently.}
}
\value{
A \code{repliapa_pd_fit}, inheriting from \code{repliapa_fit}.
}
\description{
This uses the same guide-identity exact test as \code{\link[=repliapa_fit]{repliapa_fit()}}, changing
only the feature representation from all PAS to two bins. It therefore
isolates the information added by retaining the full PAS composition.
}
