% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guide-reproducibility.R
\name{repliapa_guide_reproducibility}
\alias{repliapa_guide_reproducibility}
\title{Evaluate same-target guide reproducibility against a matched null}
\usage{
repliapa_guide_reproducibility(
  guide_effects,
  guide_data,
  match_columns,
  replicate = "replicate",
  target = "target",
  guide = "guide",
  n_boot = 2000L,
  seed = 20260812L
)
}
\arguments{
\item{guide_effects}{Long table with columns \code{target}, \code{replicate}, \code{guide},
\code{gene}, \code{pas_id}, and numeric \code{effect}.}

\item{guide_data}{One row per guide and biological replicate, including the
target label and numeric matching covariates.}

\item{match_columns}{Numeric columns used for nearest-neighbour matching.}

\item{replicate, target, guide}{Column names in \code{guide_data}.}

\item{n_boot}{Number of guide-pair and target bootstrap resamples. Set to zero
to omit confidence intervals.}

\item{seed}{Bootstrap seed. Matching itself is deterministic.}
}
\value{
A \code{repliapa_guide_reproducibility} object with gene-level pairs,
pair and target summaries, matching records, and bootstrap intervals.
}
\description{
Same-target guide pairs are formed within each biological replicate. Each
guide is matched deterministically to a guide from another target using
standardized sample-level covariates; the two matched guides must themselves
represent different targets. Cells are never resampled or treated as units.
}
