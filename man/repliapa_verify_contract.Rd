% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-contract.R
\name{repliapa_verify_contract}
\alias{repliapa_verify_contract}
\title{Verify the v1-candidate API and an optional result bundle}
\usage{
repliapa_verify_contract(contract = repliapa_api_contract(), bundle = NULL)
}
\arguments{
\item{contract}{A contract returned by \code{repliapa_api_contract()}.}

\item{bundle}{Optional \code{repliapa_result_bundle} to validate.}
}
\value{
A \code{repliapa_contract_verification} object.
}
\description{
Checks the supplied contract fingerprint against the package's frozen
contract, verifies exported function signatures and registered print
methods, and optionally validates a result bundle's required tables and
columns. The function reports every check and does not rerun inference.
}
