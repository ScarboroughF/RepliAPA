% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/null-calibration.R
\name{repliapa_ntc_calibration}
\alias{repliapa_ntc_calibration}
\title{Calibrate the method with guide-unit NTC pseudo-target contrasts}
\usage{
repliapa_ntc_calibration(
  counts,
  sample_data,
  pas_data,
  contrasts = NULL,
  guide_sizes = c(2L, 3L, 4L),
  n_contrasts = 500L,
  seed = 1L,
  gene = "gene",
  replicate = "replicate",
  guide = "guide",
  target_col = "target",
  ntc = "ntc",
  replicates = NULL,
  pseudocount = 0.5,
  min_gene_total = 20,
  max_combinations = 1e+06
)
}
\arguments{
\item{counts}{PAS-by-guide-replicate pseudobulk matrix.}

\item{sample_data}{Sample metadata aligned to columns of \code{counts}.}

\item{pas_data}{PAS metadata aligned to rows of \code{counts}. It must contain a
gene column and may contain \code{position} and \code{strand}.}

\item{contrasts}{Optional predeclared data frame with \code{contrast_id} and a
semicolon-delimited \code{pseudo_target_guides} column. Optional columns are
\code{seed} and \code{target_matched_guide_n}.}

\item{guide_sizes}{Pseudo-target guide counts used when generating contrasts.}

\item{n_contrasts}{Number of generated contrasts.}

\item{seed}{Seed used only to generate and record contrast-specific seeds.}

\item{gene, replicate, guide, target_col, ntc}{Column names.}

\item{replicates}{Optional biological replicates to include.}

\item{pseudocount}{PAS-count pseudocount used before composition and square root.}

\item{min_gene_total}{Minimum total PAS count in the tested context.}

\item{max_combinations}{Maximum number of exhaustively enumerated admissible
guide-label permutations.
Genes exceeding it abstain; labels are never sampled or split silently.}
}
\value{
A \code{repliapa_null_calibration} object with contrasts, gene results,
and calibration summaries.
}
\description{
Non-targeting guide identities are relabeled as pseudo-targets; guide cells
are never split. The exact test in \code{\link[=repliapa_fit]{repliapa_fit()}} preserves all requested
biological replicate observations belonging to each guide.
}
