% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baseline-interoperability.R
\name{repliapa_drimseq_input}
\alias{repliapa_drimseq_input}
\title{Export a frozen pseudobulk contrast as DRIMSeq input}
\usage{
repliapa_drimseq_input(
  counts,
  sample_data,
  pas_data,
  target,
  gene = "gene",
  replicate = "replicate",
  guide = "guide",
  target_col = "target",
  ntc = "ntc",
  replicates = NULL,
  pas_ids = NULL
)
}
\arguments{
\item{counts}{PAS-by-guide-replicate pseudobulk matrix.}

\item{sample_data}{Sample metadata aligned to columns of \code{counts}.}

\item{pas_data}{PAS metadata aligned to rows of \code{counts}. It must contain a
gene column and may contain \code{position} and \code{strand}.}

\item{target}{Target label to test.}

\item{gene, replicate, guide, target_col, ntc}{Column names.}

\item{replicates}{Optional biological replicates to include.}

\item{pas_ids}{Optional predeclared PAS identifiers to export. Every
identifier must exist; the function never silently intersects site sets.}
}
\value{
A \code{repliapa_drimseq_input} object containing \code{counts}, \code{samples},
\code{design}, and an explicit \code{accounting} table. The \code{counts} and \code{samples}
components can be passed directly to \code{DRIMSeq::dmDSdata()}.
}
\description{
Creates the two tables expected by \code{DRIMSeq::dmDSdata()} without fitting or
filtering the baseline. Target and non-targeting guide identities remain
attached to their biological-replicate rows. Only guides observed in every
requested replicate are exported.
}
