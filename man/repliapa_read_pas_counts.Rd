% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/container-input.R
\name{repliapa_read_pas_counts}
\alias{repliapa_read_pas_counts}
\title{Read PAS counts from matrices or single-cell containers}
\usage{
repliapa_read_pas_counts(
  x,
  cell_data = NULL,
  pas_data = NULL,
  assay = NULL,
  layer = "counts"
)
}
\arguments{
\item{x}{A base matrix, \code{Matrix}, Seurat object,
\code{SingleCellExperiment}, or \code{SummarizedExperiment}.}

\item{cell_data}{Cell metadata for matrix input. For container input, an
optional replacement for the object's column metadata.}

\item{pas_data}{PAS metadata for matrix input. For container input, an
optional replacement for the object's feature metadata.}

\item{assay}{Explicit PAS assay name for container inputs. Ignored for a
matrix input.}

\item{layer}{Seurat count layer or slot. The default is \code{"counts"}.}
}
\value{
A \code{repliapa_input} containing aligned \code{counts}, \code{cell_data}, and
\code{pas_data}, plus input provenance.
}
\description{
Standardizes a PAS-by-cell count matrix, cell metadata, and PAS metadata
without detecting or modifying PASs. For container inputs the PAS assay must
be named explicitly; this fail-closed rule prevents an RNA assay from being
analysed as poly(A)-site counts.
}
