% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input-audit.R
\name{repliapa_audit_input}
\alias{repliapa_audit_input}
\title{Audit a guide-by-replicate PAS pseudobulk contract}
\usage{
repliapa_audit_input(
  counts,
  sample_data,
  pas_data,
  development_replicates,
  gene = "gene",
  replicate = "replicate",
  guide = "guide",
  target = "target",
  ntc = "ntc",
  cell_n = "cell_n",
  min_cells = 20L,
  min_polya_count = 150000L,
  min_target_guides = 2L,
  usable_total = 20L,
  usable_nonzero = 2L,
  strong_total = 200L,
  strong_nonzero = 10L,
  seqname = "seqnames",
  start = "start",
  end = "end"
)
}
\arguments{
\item{counts}{PAS-by-sample-unit pseudobulk count matrix.}

\item{sample_data}{Sample metadata aligned to columns of \code{counts}.}

\item{pas_data}{PAS metadata aligned to rows of \code{counts}.}

\item{development_replicates}{Replicate labels allowed to define PAS support
tiers. Validation or external replicates must not be supplied here.}

\item{gene, replicate, guide, target, ntc, cell_n}{Column names.}

\item{min_cells, min_polya_count}{Predeclared sample-unit qualification rules.}

\item{min_target_guides}{Minimum independent qualified guides required in
every biological replicate for primary target eligibility.}

\item{usable_total, usable_nonzero}{PAS-A2 lower bounds.}

\item{strong_total, strong_nonzero}{PAS-A3 lower bounds.}

\item{seqname, start, end}{Optional PAS coordinate column names. If all are
present, exact duplicate coordinates within a gene are marked as conflicts.}
}
\value{
A \code{repliapa_input_audit} containing contract checks, sample-unit,
guide, target, PAS, and gene adequacy tables plus immutable settings.
}
\description{
Performs the pre-fit data-contract and adequacy audit used by RepliAPA. The
function does not alter counts, choose favorable targets, or fit an outcome
model. PAS support tiers are computed only from predeclared development
replicates and qualified sample units.
}
